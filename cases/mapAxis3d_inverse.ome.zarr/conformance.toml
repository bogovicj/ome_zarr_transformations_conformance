description = "A 3d mapAxis cyclic permutation (not its own inverse). (inverse)"
should_error = false
absolute_tolerance = 1e-06
relative_tolerance = 0.001

[source]
name = "output"
coordinates = [
    [
        3,
        1,
        2,
    ],
]

[target]
name = "input"
coordinates = [
    [
        1,
        2,
        3,
    ],
]
